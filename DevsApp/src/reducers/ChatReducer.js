const initialState ={
  uid:0,
  status:0
};

const ChatReducer = (state = initialState, action) => {
  switch(action.state){
    case 'checkUser':
      break;
  }
  return state;
}

export default ChatReducer;